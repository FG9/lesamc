{
  "routes": [
    {
      "method": "GET",
      "path": "/blog",
      "handler": "Blog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/blog/:_id",
      "handler": "Blog.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/blog",
      "handler": "Blog.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/blog/:_id",
      "handler": "Blog.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/blog/:_id",
      "handler": "Blog.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}